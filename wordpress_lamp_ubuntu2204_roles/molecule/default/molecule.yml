---
driver:
  name: docker
platforms:
  - name: instance
    image: trfore/docker-ubuntu2404-systemd
    pre_build_image: true
provisioner:
  name: ansible
  playbooks:
    converge: ../../playbook.yml
verifier:
  name: ansible
